서버에 요청을 보낼 때는 주소를 통해 요청의 내용을 표헌.
요청 내용이 주소를 통해 표현되므로 서버가 이해하기 쉬운 주소를 사용하는 것이 좋다.

REST API는 서버의 자원을 정의하고 자원에 대한 주소를 지정하는 방법을 가리킨다.
많은 규칙들이 있지만, 현실적으로 모든 규칙을 지키는 것은 현실적으로 어렵다.

주소의 명사 + GET, POST, PUT, DELETE 메서드를 사용한다.
// GET 메서드는 브라우저에서 캐싱(기억)할 수 있다. 